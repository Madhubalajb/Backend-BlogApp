const listHelper = require('../utils/list_helper')
const blogLists = require('../utils/blogLists')

describe('favorite Blog', () => {
    test('favorite Blog test', () => {
        expect(listHelper.favoriteBlog(lists)).toEqual()
    })
})